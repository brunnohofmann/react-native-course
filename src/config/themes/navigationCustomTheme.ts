import {DefaultTheme} from '@react-navigation/native';

const MyTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#36802d',
    background: '#D5D6DE',
  },
};

export default MyTheme;
